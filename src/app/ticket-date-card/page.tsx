import React from "react";

import TicketsSelection from "@/components/ui/ticket-selection";

const page = () => {
  return (
    <div>
      <TicketsSelection />
    </div>
  );
};

export default page;
